# 蒙德里安风格科学计算器

一个基于 Vue 3 + Vite 构建的蒙德里安/构成主义设计风格科学计算器。

## ✨ 功能特性

- 🎨 **蒙德里安/构成主义设计风格** - 粗黑线条、红黄蓝色块、几何构成
- 🌈 **明亮轻快的背景** - 彩色摩斯波点 + 毛玻璃效果
- 🔢 **完整的科学计算功能** - 加减乘除、三角函数、对数、幂次、常数等
- ⌨️ **键盘和鼠标双重交互** - 支持完整的键盘快捷键
- 🔄 **连续累积运算** - 计算结果自动作为下一次运算的起点
- 📱 **响应式设计** - 适配不同屏幕尺寸

## 🚀 快速开始

### 环境要求

- Node.js >= 22.0.0
- npm 或 yarn 或 pnpm

### 安装依赖

```bash
npm install
# 或
yarn install
# 或
pnpm install
```

### 开发模式

```bash
npm run dev
# 或
yarn dev
# 或
pnpm dev
```

访问 http://localhost:3000

### 构建生产版本

```bash
npm run build
# 或
yarn build
# 或
pnpm build
```

构建产物在 `dist/` 目录

### 预览生产构建

```bash
npm run preview
# 或
yarn preview
# 或
pnpm preview
```

## 📦 部署到腾讯云 CloudBase

### 1. 安装 CloudBase CLI

```bash
npm install -g @cloudbase/cli
```

### 2. 登录 CloudBase

```bash
tcb login
```

### 3. 初始化项目（如果还没初始化）

```bash
tcb init
```

### 4. 构建项目

```bash
npm run build
```

### 5. 部署到 CloudBase

```bash
tcb hosting deploy dist -e your-env-id
```

或者使用配置文件 `cloudbase.json`：

```bash
tcb hosting deploy dist
```

### 6. 配置自定义域名（可选）

在 CloudBase 控制台配置你的自定义域名。

## ⌨️ 键盘快捷键

- **数字**：0-9
- **小数点**：.
- **运算符**：+ - * /
- **括号**：( )
- **常数**：e (Euler's number), p (π)
- **删除**：Backspace (删除一位), Delete (清空)
- **等号**：Enter 或 =

## 🛠️ 技术栈

- **Vue 3** - 渐进式 JavaScript 框架
- **Vite** - 下一代前端构建工具
- **Node.js 22** - JavaScript 运行时
- **纯 CSS** - 无 UI 框架依赖

## 📁 项目结构

```
.
├── src/
│   ├── components/
│   │   ├── Calculator.vue      # 计算器主组件
│   │   └── Calculator.css      # 计算器样式
│   ├── App.vue                  # 根组件
│   ├── main.js                  # 入口文件
│   └── style.css                # 全局样式
├── index.html                   # HTML 模板
├── package.json                 # 项目配置
├── vite.config.js               # Vite 配置
├── cloudbase.json               # CloudBase 部署配置
└── README.md                    # 项目说明
```

## 📝 开发说明

### 修改计算器逻辑

编辑 `src/components/Calculator.vue` 文件。

### 修改样式

- 全局样式：`src/style.css`
- 计算器样式：`src/components/Calculator.css`

### 添加新功能

1. 在 `Calculator.vue` 中添加新的按钮和方法
2. 更新键盘事件处理函数
3. 更新样式（如需要）

## 📄 许可证

MIT License

## 🙏 致谢

设计灵感来源于蒙德里安（Piet Mondrian）和构成主义艺术运动。

