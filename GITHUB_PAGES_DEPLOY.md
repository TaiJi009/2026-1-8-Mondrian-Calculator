# GitHub Pages 一键部署指南

## 🚀 快速部署步骤

### 1. 准备 GitHub 仓库

如果还没有 GitHub 仓库，请先创建：

```bash
# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: 蒙德里安计算器"

# 添加远程仓库（替换为你的仓库地址）
git remote add origin https://github.com/你的用户名/2026-1-8-Mondrian-Calculator.git

# 推送到 GitHub
git branch -M main
git push -u origin main
```

### 2. 启用 GitHub Pages

1. 进入你的 GitHub 仓库
2. 点击 `Settings`（设置）
3. 在左侧菜单找到 `Pages`（页面）
4. 在 `Source`（源）部分，选择 `GitHub Actions`
5. 保存设置

### 3. 一键部署

#### 方法一：自动部署（推荐）

只需推送代码到 `main` 或 `master` 分支：

```bash
git add .
git commit -m "更新代码"
git push origin main
```

GitHub Actions 会自动：
- 检测到代码推送
- 安装依赖
- 构建项目
- 部署到 GitHub Pages

#### 方法二：手动触发

1. 进入仓库的 `Actions`（操作）标签页
2. 选择 `Deploy to GitHub Pages` 工作流
3. 点击 `Run workflow`（运行工作流）按钮
4. 选择分支（通常是 `main`）
5. 点击 `Run workflow` 确认

### 4. 查看部署状态

- 在 `Actions` 标签页查看部署进度
- 绿色勾号表示部署成功
- 红色叉号表示部署失败（点击查看错误信息）

### 5. 访问你的网站

部署成功后，访问地址为：
```
https://你的用户名.github.io/2026-1-8-Mondrian-Calculator/
```

> ⏱️ **注意**：首次部署可能需要 1-2 分钟，请耐心等待。

## 🔧 自定义配置

### 修改仓库名

如果仓库名不是 `2026-1-8-Mondrian-Calculator`，需要修改 `vite.config.js`：

```javascript
const base = process.env.VITE_BASE || (process.env.NODE_ENV === 'production' ? '/你的仓库名/' : '/')
```

或者，GitHub Actions 会自动根据仓库名设置，无需手动修改。

### 使用自定义域名

1. 在仓库的 `Settings` → `Pages` 中设置自定义域名
2. 修改 `vite.config.js` 中的 base 为 `/`（根路径）
3. 重新部署

## ❓ 常见问题

### Q: 部署后页面空白？

A: 检查以下几点：
- 确认 GitHub Pages 的 Source 设置为 `GitHub Actions`
- 查看 Actions 中的构建日志，确认构建成功
- 确认访问地址中的路径正确（包含仓库名）

### Q: 如何更新网站？

A: 只需推送新代码到 main 分支，GitHub Actions 会自动重新部署。

### Q: 部署失败怎么办？

A: 
1. 查看 `Actions` 标签页中的错误信息
2. 确认 Node.js 版本兼容（需要 >= 22.0.0）
3. 检查 `package.json` 中的依赖是否正确

### Q: 如何回滚到之前的版本？

A: 在 GitHub 中恢复到之前的提交，然后推送即可触发重新部署。

## 📚 更多信息

- [GitHub Pages 官方文档](https://docs.github.com/en/pages)
- [GitHub Actions 文档](https://docs.github.com/en/actions)
